// Standalone build — NOT included in the main UtilsLibs composite build.
// The IntelliJ Platform Plugin SDK has different requirements from Android/KMP.
//
// To build and run locally:
//   cd intellij-plugin && ./gradlew runIde
//
// To produce a distributable ZIP:
//   cd intellij-plugin && ./gradlew buildPlugin
//
// The output lands in intellij-plugin/build/distributions/

rootProject.name = "kolt-intellij-plugin"
