<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <provider
            android:name="io.github.appspiriment.kolt.composeutils.utils.KoltFileProvider"
            android:authorities="${applicationId}.kolt.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/kolt_photo_picker_path" />
        </provider>
    </application>

</manifest>